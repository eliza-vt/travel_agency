program Project2;

uses
  Forms,
  Server in 'Server.pas' {Form1},
  Project2_TLB in 'Project2_TLB.pas',
  Unit1 in 'Unit1.pas' {MyServer: TRemoteDataModule} {MyServer: CoClass};

{$R *.TLB}

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
